module.exports.handle = function (msg) {
	/*
	 	Necesitas hacer toda la logica para reenviar el data a cada uno
	 	de los servidores, además de definir como vas a almacenar el
	 	log de replicacion para saber que cosas ya fueron replicadas
	 	y que no
	*/
	w.debug('I will handle this: %s', msg);
}